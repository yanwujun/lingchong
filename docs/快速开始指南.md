# 快速开始指南 🚀

欢迎使用桌面灵宠开发框架！本指南将帮助你快速上手项目开发。

## 📋 准备工作

### 1. 环境检查

确保你的系统满足以下要求：
- ✅ Python 3.8 或更高版本
- ✅ Windows 10/11（推荐）
- ✅ pip 包管理器

检查Python版本：
```bash
python --version
```

### 2. 安装依赖

```bash
# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖包
pip install -r requirements.txt
```

### 3. 验证安装

```bash
# 运行主程序
python main.py
```

如果看到欢迎信息，说明环境配置成功！

---

## 🎯 开发流程建议

### 阶段一：理解项目结构（1-2小时）

1. **阅读文档**
   - [ ] 阅读 `开发需求文档.md`
   - [ ] 阅读 `README.md`
   - [ ] 浏览 `assets/README.md`

2. **熟悉代码**
   ```bash
   # 浏览各个模块
   src/
   ├── pet_window.py      # 从这里开始！
   ├── todo_window.py
   ├── database.py
   ├── reminder.py
   ├── settings_window.py
   └── utils.py
   ```

3. **运行测试代码**
   ```bash
   # 测试宠物窗口
   python src/pet_window.py
   
   # 测试待办窗口
   python src/todo_window.py
   
   # 测试数据库
   python src/database.py
   
   # 测试提醒系统
   python src/reminder.py
   
   # 测试设置窗口
   python src/settings_window.py
   
   # 测试工具函数
   python src/utils.py
   ```

### 阶段二：准备资源文件（2-4小时）

1. **寻找或制作宠物图片**
   
   **方案A：使用现成资源**
   - 访问 [OpenGameArt](https://opengameart.org/)
   - 搜索 "sprite" 或 "character"
   - 下载合适的角色动画
   
   **方案B：参考开源项目**
   - DyberPet: https://github.com/ChaozhongLiu/DyberPet
   - 查看其资源文件组织方式
   - 学习如何制作和使用动画
   
   **方案C：自己制作**
   - 使用 Aseprite 或 Piskel
   - 制作简单的像素艺术动画
   - 导出为 GIF 格式

2. **放置资源文件**
   ```
   assets/
   └── images/
       └── default/
           ├── idle.gif      # 必需
           ├── walk.gif      # 必需
           ├── sleep.gif     # 可选
           ├── happy.gif     # 可选
           └── alert.gif     # 可选
   ```

3. **修改代码加载资源**
   ```python
   # 在 src/pet_window.py 中
   def load_animation(self, animation_name):
       animation_path = f"assets/images/default/{animation_name}.gif"
       if os.path.exists(animation_path):
           self.movie = QMovie(animation_path)
           self.pet_label.setMovie(self.movie)
           self.movie.start()
   ```

### 阶段三：实现核心功能（1-2周）

#### 第1天：完善宠物窗口

```python
# TODO 列表：
1. [ ] 实现 load_animation() 方法
2. [ ] 测试多种动画切换
3. [ ] 优化拖拽性能
4. [ ] 添加屏幕边缘检测
5. [ ] 实现重力效果（可选）
```

**测试方法：**
```bash
python src/pet_window.py
```

#### 第2-3天：集成数据库

```python
# TODO 列表：
1. [ ] 测试数据库基本操作
2. [ ] 创建任务CRUD功能
3. [ ] 实现数据持久化
4. [ ] 添加数据备份功能
```

**测试方法：**
```bash
# 运行数据库测试
python src/database.py

# 检查是否生成了数据库文件
ls data/tasks.db  # Linux/Mac
dir data\tasks.db  # Windows
```

#### 第4-5天：完善待办窗口

```python
# TODO 列表：
1. [ ] 连接数据库到UI
2. [ ] 实现任务添加功能
3. [ ] 实现任务编辑功能
4. [ ] 实现任务删除功能
5. [ ] 添加搜索和筛选
```

**测试方法：**
```bash
python src/todo_window.py
```

#### 第6-7天：实现提醒系统

```python
# TODO 列表：
1. [ ] 实现定时检查机制
2. [ ] 创建提醒弹窗
3. [ ] 连接宠物动画
4. [ ] 添加音效（可选）
5. [ ] 测试提醒准确性
```

**测试方法：**
```python
# 创建测试任务，设置1分钟后提醒
from datetime import datetime, timedelta
from src.database import Database

db = Database()
remind_time = (datetime.now() + timedelta(minutes=1)).strftime("%Y-%m-%d %H:%M:%S")
db.add_task("测试提醒", remind_time=remind_time)
```

#### 第8-10天：整合所有模块

修改 `main.py`：

```python
def main():
    app = QApplication(sys.argv)
    
    # 1. 加载配置
    from src.utils import load_config
    config = load_config()
    
    # 2. 初始化数据库
    from src.database import Database
    db = Database(config['Database']['db_path'])
    
    # 3. 创建宠物窗口
    from src.pet_window import PetWindow
    pet_window = PetWindow(config)
    pet_window.show()
    
    # 4. 创建待办窗口（隐藏）
    from src.todo_window import TodoWindow
    todo_window = TodoWindow(db)
    
    # 5. 启动提醒系统
    from src.reminder import ReminderSystem
    reminder = ReminderSystem(db, pet_window)
    reminder.start()
    
    # 6. 创建系统托盘
    # TODO
    
    sys.exit(app.exec_())
```

### 阶段四：测试和优化（3-5天）

1. **功能测试清单**
   - [ ] 宠物能正常显示和拖动
   - [ ] 动画能正常播放和切换
   - [ ] 可以添加、编辑、删除任务
   - [ ] 提醒功能准时触发
   - [ ] 设置能正常保存和加载
   - [ ] 右键菜单功能正常

2. **性能测试**
   ```python
   # 检查CPU和内存占用
   # 使用任务管理器或：
   import psutil
   import os
   
   process = psutil.Process(os.getpid())
   print(f"内存占用: {process.memory_info().rss / 1024 / 1024:.2f} MB")
   print(f"CPU使用: {process.cpu_percent()}%")
   ```

3. **用户体验优化**
   - [ ] 界面响应流畅
   - [ ] 动画自然不卡顿
   - [ ] 操作符合直觉
   - [ ] 错误提示友好

---

## 🔧 常见问题

### Q1: 如何调试PyQt界面？

```python
# 在代码中添加调试输出
print(f"[DEBUG] 当前位置: {self.pos()}")
print(f"[DEBUG] 窗口大小: {self.size()}")

# 使用PyQt的调试工具
from PyQt5.QtCore import qDebug
qDebug("调试信息")
```

### Q2: 动画不显示怎么办？

检查清单：
1. 确认文件路径正确
2. 确认文件格式（GIF）
3. 确认文件没有损坏
4. 检查标签大小是否足够

```python
# 调试代码
import os
animation_path = "assets/images/default/idle.gif"
print(f"文件存在: {os.path.exists(animation_path)}")
print(f"文件大小: {os.path.getsize(animation_path)} bytes")
```

### Q3: 窗口不透明怎么办？

```python
# 确保设置了正确的窗口属性
self.setAttribute(Qt.WA_TranslucentBackground)
self.setWindowFlags(Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint)

# 检查样式表
self.setStyleSheet("background: transparent;")
```

### Q4: 如何打包成exe？

```bash
# 安装PyInstaller
pip install pyinstaller

# 打包（单文件）
pyinstaller --onefile --windowed --icon=assets/icons/app_icon.ico main.py

# 打包（带资源）
pyinstaller --windowed --add-data "assets;assets" --add-data "config.ini;." main.py
```

---

## 📚 学习资源

### PyQt5教程
- [PyQt5官方文档](https://doc.qt.io/qtforpython/)
- [PyQt5中文教程](https://maicss.gitbook.io/pyqt5/)
- [Real Python PyQt教程](https://realpython.com/python-pyqt-gui-calculator/)

### 桌面宠物参考
- [DyberPet源码](https://github.com/ChaozhongLiu/DyberPet) - 非常完善的实现
- [Pet-GPT源码](https://github.com/Hanzoe/Pet-GPT) - 集成AI的例子
- [Desktop Pet源码](https://github.com/cgy2003/desktop-pet) - 简单实现

### 资源制作
- [Piskel](https://www.piskelapp.com/) - 在线像素艺术编辑器
- [Aseprite教程](https://www.aseprite.org/docs/) - 专业像素艺术工具

---

## 💡 开发技巧

### 1. 使用版本控制

```bash
# 初始化Git仓库
git init

# 添加文件
git add .

# 提交
git commit -m "Initial commit"

# 创建功能分支
git checkout -b feature/pet-animation
```

### 2. 模块化测试

每个模块都可以独立运行测试：
```bash
# 单独测试某个模块
python src/pet_window.py
```

### 3. 逐步迭代

不要试图一次实现所有功能，建议的顺序：
1. ✅ 基础窗口显示
2. ✅ 简单动画
3. ✅ 基本交互
4. ✅ 数据库集成
5. ✅ 待办功能
6. ✅ 提醒系统
7. ✅ 设置功能
8. ✅ 系统托盘
9. ✅ 高级功能

### 4. 记录和注释

```python
# 好的注释示例
def load_animation(self, animation_name):
    """
    加载指定的动画
    
    Args:
        animation_name: 动画名称（如 'idle', 'walk'）
    
    Returns:
        bool: 是否加载成功
    """
    # 实现代码...
```

---

## 🎉 下一步

完成基础开发后，可以考虑：

1. **增强功能**
   - 添加更多宠物皮肤
   - 实现宠物成长系统
   - 集成AI对话（参考Pet-GPT）
   - 添加番茄钟功能

2. **优化体验**
   - 添加音效和背景音乐
   - 实现更流畅的动画过渡
   - 优化性能和内存占用
   - 添加多语言支持

3. **分享项目**
   - 上传到GitHub
   - 编写详细的README
   - 制作使用教程视频
   - 收集用户反馈

---

## 📞 获取帮助

遇到问题时：

1. 查看 `开发需求文档.md`
2. 阅读代码注释
3. 运行模块测试代码
4. 搜索相关PyQt5文档
5. 参考开源项目实现

---

祝你开发顺利！🚀

如有问题，欢迎提Issue讨论。

